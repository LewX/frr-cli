frr-cli is vtysh part of frr 7.2-0

install dependency deb libyang:
    https://github.com/CESNET/libyang.git
or install release deb:
    sudo dpkg -i release/libyang-dev_1.0.73_amd64.deb

build & install frr-cli:
    cd frr-cli && make && make install
or install release deb:
    sudo dpkg -i release/libra-cli_1.0.1_amd64.deb

how to use:
    libra-cli

frr source:
    https://github.com/FRRouting/frr
